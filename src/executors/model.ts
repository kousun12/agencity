import type { JsonValue } from "../domain/json.ts";import { ValidationError,type ModelConfiguration,type Usage } from "../domain/index.ts";import type { EffectExecutor,ExecutionResult } from "./contract.ts";import { result } from "./contract.ts";
export interface ModelResponse {readonly text:string;readonly finishReason:string;readonly usage:Usage;}
export interface ModelProvider {readonly name:string;complete(context:JsonValue,configuration:ModelConfiguration,signal:AbortSignal):Promise<ModelResponse>;}
export class EchoModelProvider implements ModelProvider {readonly name="echo";async complete(context:JsonValue,_configuration:ModelConfiguration,signal:AbortSignal):Promise<ModelResponse>{if(signal.aborted)throw new DOMException("Aborted","AbortError");let text="";if(context&&typeof context==="object"&&!Array.isArray(context)){const messages=context.messages;if(Array.isArray(messages)){const last=[...messages].reverse().find(m=>m&&typeof m==="object"&&!Array.isArray(m)&&m.role==="user");if(last&&typeof last==="object"&&!Array.isArray(last)&&typeof last.content==="string")text=`Echo: ${last.content}`;}}return{text:text||"Echo model completed.",finishReason:"stop",usage:{inputTokens:Math.ceil(JSON.stringify(context).length/4),outputTokens:Math.ceil((text||"Echo model completed.").length/4),costUsd:0}};}}
export interface OpenAICompatibleOptions {readonly baseUrl:string;readonly apiKey:()=>string|undefined;readonly providerName?:string;}
export class OpenAICompatibleProvider implements ModelProvider {readonly name:string;constructor(readonly options:OpenAICompatibleOptions){this.name=options.providerName??"openai";}async complete(context:JsonValue,configuration:ModelConfiguration,signal:AbortSignal):Promise<ModelResponse>{const key=this.options.apiKey();if(!key)throw new ValidationError(`Credential unavailable for ${this.name}`);const contextObject=context&&typeof context==="object"&&!Array.isArray(context)?context:{};const messages=Array.isArray(contextObject.messages)?contextObject.messages:[];const response=await fetch(`${this.options.baseUrl.replace(/\/$/,"")}/chat/completions`,{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${key}`},body:JSON.stringify({model:configuration.model,messages,temperature:configuration.temperature,max_tokens:configuration.maxOutputTokens}),signal});if(!response.ok)throw new Error(`Model HTTP ${response.status}: ${await response.text()}`);const body=await response.json() as {choices?:Array<{message?:{content?:string};finish_reason?:string}>;usage?:{prompt_tokens?:number;completion_tokens?:number};cost_usd?:number};const choice=body.choices?.[0];return{text:choice?.message?.content??"",finishReason:choice?.finish_reason??"stop",usage:{inputTokens:body.usage?.prompt_tokens??0,outputTokens:body.usage?.completion_tokens??0,costUsd:body.cost_usd??0}};}}
function parse(input:JsonValue):{context:JsonValue;configuration:ModelConfiguration}{if(!input||typeof input!=="object"||Array.isArray(input))throw new ValidationError("Model input must be an object");const context=input.context,config=input.configuration;if(context===undefined||!config||typeof config!=="object"||Array.isArray(config)||typeof config.provider!=="string"||typeof config.model!=="string")throw new ValidationError("Model input requires context and configuration");return{context,configuration:config as unknown as ModelConfiguration};}
export class ModelExecutor implements EffectExecutor {readonly name="model";readonly #providers=new Map<string,ModelProvider>();constructor(providers:readonly ModelProvider[]){for(const p of providers)this.#providers.set(p.name,p);}async execute(req:Parameters<EffectExecutor["execute"]>[0],ctx:Parameters<EffectExecutor["execute"]>[1]):Promise<ExecutionResult>{if(req.operation!=="complete")return result("failed",undefined,`Unsupported model operation: ${req.operation}`);try{const {context,configuration}=parse(req.input),provider=this.#providers.get(configuration.provider);if(!provider)return result("failed",undefined,`Unknown model provider: ${configuration.provider}`);const response=await provider.complete(context,configuration,ctx.signal);return result("succeeded",response as unknown as JsonValue);}catch(error){if(ctx.signal.aborted||error instanceof DOMException&&error.name==="AbortError")return result("cancelled",undefined,"Model call cancelled");return result("failed",undefined,error instanceof Error?error.message:String(error));}}}
